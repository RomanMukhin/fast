%h3 Listing users
.row
  .span1 Admin
  .span2 Name
  .span2 City
- users.each do |user|
  .row#index[user]
    .span1= user.admin
    .span2= user.name
    .span2= user.city
    .span1= link_to 'Show', user
    .span1= link_to 'Edit', edit_user_path(user)
    .span1= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }